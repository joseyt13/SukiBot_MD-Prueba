const handler = async (m, { conn, args, usedPrefix, command }) => { // Validación if (!args[0]) { return m.reply(`❗ Usa el comando así:\n\n${usedPrefix}${command} <número>\nEjemplo: ${usedPrefix}${command} 573001234567`); } let numero = args[0].replace(/\D/g, '') + '@s.whatsapp.net'; // Mensaje que se enviará al soporte const texto = `📩 *Solicitud de soporte*\n\n🧑 Usuario: wa.me/${m.sender.split('@')[0]}\n📞 Soporte solicitado para: wa.me/${args[0]}\n🕒 Fecha: ${new Date().toLocaleString()}`; try { await conn.sendMessage(numero, { text: texto }); m.reply(`✅ Soporte enviado correctamente a ${args[0]}`); } catch (e) { console.error(e); m.reply(`❌ Error al enviar el mensaje a ${args[0]}. Asegúrate de que el número está en WhatsApp y que el bot puede enviarle mensajes.`); } }; handler.command = /^soporte$/i; handler.help = ['soporte <número>']; handler.tags = ['utilidad']; module.exports = handler; 
